<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:property="clr-namespace:ReiEditor.ViewModels.Windows.Editor.Monitor.Drawers.Property"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ReiEditor.Views.Windows.Editor.Monitor.Drawers.Property.BooleanPropertyView"
             x:DataType="property:BooleanPropertyViewModel">

    <Design.DataContext>
        <property:BooleanPropertyViewModel />
    </Design.DataContext>

    <Border
        Padding="1">
        <Grid ColumnDefinitions="Auto, *">
            <TextBlock
                Grid.Column="0"
                Width="120"
                VerticalAlignment="Center"
                FontSize="14"
                Foreground="{StaticResource Gray_4}"
                Text="{Binding PropertyName.Value}" />

            <CheckBox Grid.Column="1" HorizontalAlignment="Left"
                      Cursor="Hand"
                      IsChecked="{Binding Value, Mode=TwoWay}" />
        </Grid>
    </Border>
</UserControl>