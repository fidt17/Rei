#ifdef VERTEX
    void main()
    {
        REI_CalculateFragPosAndNormal();
        REI_CalculatePointLightPositions();
    }
#endif

#ifdef FRAGMENT
    uniform float _Shininess;
    uniform vec4 _Color;
    uniform sampler2D _MainTex;

    void main()
    {
        vec4 ambient = REI_CalculateAmbientLight();
        vec4 pointLight = REI_CalculatePointLight(_Shininess);
        vec4 texColor = REI_Sample(_MainTex);

        FragColor = texColor * _Color * (ambient + pointLight);
    }
#endif
